# ===================================
#  Form Field Definitions
# ===================================

fields:
    api_key:
        label: API Key
        type: text
        comment: Enter the API key for communication with the server